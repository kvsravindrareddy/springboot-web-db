server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysqldb.cnayglq3wr2e.us-east-2.rds.amazonaws.com:3306/mysqldb
    username: mysqldb
    password: mysql123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

management:
  endpoint:
    health:
      show-components: ALWAYS
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'